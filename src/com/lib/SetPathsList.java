package com.lib;

final class SetPathsList {
//    static Path byFileName(String currentDirectory,
//                           String userName,
//                           String fileName) {
        // FIXME - Don't assume the use of .toString() or the use of Paths.get()
        //  -  on that string is correct create a util class with this method
        //  -  and test class to check
        //  -  Make sure the paths point to existing files
        //  -  Don't just check if the string is correct
//        return Paths.get(InstallRoot.getInstallRootPath(
//                currentDirectory,
//                userName).toString()
//                + "\\" + fileName);
//    }
}
